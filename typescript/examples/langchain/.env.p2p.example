# =============================================================================
# P2P Gasless Payments - Environment Configuration
# =============================================================================

# -----------------------------------------------------------------------------
# Required for ALL examples
# -----------------------------------------------------------------------------

# OpenAI API Key (for LangChain agent)
OPENAI_API_KEY=sk-proj-...

# P2P Gasless Topic ID (shared public topic for gasless payments)
# Create one with: hedera topic create --memo "Gasless Payments"
P2P_GASLESS_TOPIC_ID=0.0.YOUR_TOPIC_ID

# -----------------------------------------------------------------------------
# For USER examples (p2p-gasless-user.ts)
# -----------------------------------------------------------------------------

# User account (sender - doesn't need HBAR for fees!)
ACCOUNT_ID=0.0.YOUR_ACCOUNT
PRIVATE_KEY=0x...  # Your ECDSA private key

# Recipient account for test payments
RECIPIENT_ACCOUNT_ID=0.0.12345

# Optional: Token ID if testing token transfers
TOKEN_ID=0.0.67890

# -----------------------------------------------------------------------------
# For SPONSOR examples (p2p-gasless-sponsor.ts)
# -----------------------------------------------------------------------------

# Sponsor account (must have sufficient HBAR balance)
# Recommended: 100+ HBAR for production, 10+ for testing
SPONSOR_ACCOUNT_ID=0.0.YOUR_SPONSOR_ACCOUNT
SPONSOR_PRIVATE_KEY=0x...  # Sponsor's ECDSA private key

# Sponsor Fee Configuration
# Fee model: 'flat' or 'percentage'
SPONSOR_FEE_MODEL=percentage

# Flat fee (in HBAR) - used if SPONSOR_FEE_MODEL=flat
SPONSOR_FLAT_FEE=0.01

# Percentage fee (1 = 1%) - used if SPONSOR_FEE_MODEL=percentage
SPONSOR_PERCENTAGE_FEE=1

# Minimum HBAR balance to keep operating
SPONSOR_MIN_BALANCE=10

# Poll interval for checking new payments (milliseconds)
SPONSOR_POLL_INTERVAL=5000

# Max payments to relay per batch
SPONSOR_MAX_BATCH=5

# Run mode: 'true' for manual control, 'false' for auto-relay
MANUAL_MODE=false

# -----------------------------------------------------------------------------
# Optional: Network Configuration
# -----------------------------------------------------------------------------

# Network (testnet or mainnet)
# HEDERA_NETWORK=testnet

# Custom mirrornode URL (optional)
# MIRROR_NODE_URL=https://testnet.mirrornode.hedera.com
